\ProvidesPackage{custom}

% よく使うパッケージの読み込み
\usepackage{amsmath} % 数式
\usepackage{amsfonts} % 数式のフォント
\usepackage{amssymb} % 複雑な数式
\usepackage[dvipdfmx]{graphicx} % 画像の処理
\usepackage{here} % 図の配置
\usepackage{mathtools} % 数式
\usepackage{subcaption} % 複数の図の配置
\usepackage{siunitx} % SI単位系

% 余白や行間
\usepackage[top=3cm, bottom=3cm, left=3cm, right=3cm]{geometry}

% リンクの色
\usepackage[dvipdfmx,hyperref,caption]{xcolor}
\usepackage[dvipdfmx]{hyperref}
\usepackage{pxjahyper}
\hypersetup{
    linkbordercolor=cyan,
    citebordercolor=cyan
}

% bibファイルの設定
\usepackage[
    backend=biber,  % biberを使う
    style=chem-angew,
    sorting=none,  % 参照順にソート
    giveninits=true  % given nameを短縮
]{biblatex}
\addbibresource{references.bib}

% マクロ
% \renewcommand{\eqref}[1]{式~(\ref{#1})}
% \newcommand{\figref}[1]{~\ref{#1}}
% \newcommand{\tbref}[1]{表~\ref{#1}}
% \newcommand{\secref}[1]{\ref{#1}~節}
